import { SplashScreenBehaviorDirective } from './splash-screen-behavior.directive';

describe('SplashScreenBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new SplashScreenBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
