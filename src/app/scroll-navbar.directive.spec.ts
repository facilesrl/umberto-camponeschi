import { ScrollNavbarDirective } from './scroll-navbar.directive';

describe('ScrollNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
